// Mirrors the constants in src/components/SEO.js. Duplicated rather than
// imported because SEO.js doesn't export them — this file's only job is
// structured data, so it stays a self-contained, deletable unit.
export const SITE_NAME = "Path To Mexico";
export const SITE_URL = "https://pathtomexico.com";
export const DEFAULT_OG_IMAGE = `${SITE_URL}/og-image.jpg?v=3`;
